export { NunjucksEngine } from './engine'
export type { NunjucksEngineOptions } from './engine'

// Exposed for docs generators and catalog-in-sync checks. Not part of
// the stable template-authoring surface.
export { buildFilters } from './filters'
export { buildGlobals } from './globals'
